#include <string>
#include <vector>

using namespace std;

int solution(vector<vector<int>> triangle) {
    int answer = 0;
    int temp = triangle[0][0];
    vector<vector<int>> t;
    t.push_back(vector<int>())
    return answer;
}